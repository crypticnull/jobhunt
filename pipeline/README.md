# pipeline

Asset intake and data validation: the filename token parser, the media
ingest probe, and the validator for the whole `/data` tree. Validation
gets its first cut in milestone 2, ingest in milestone 10. Today it
carries the repo-guarantee tests: the privacy split must hold.
