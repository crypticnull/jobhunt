# 2026-09-02, session seventeen

Shipped:
- setup ran green on Matt's machine for the first time: tasks registered, first poll done, company list pushed
- discovery rewritten against that run's real output: a tight require_any list, excluded titles, generic company names dropped, HN comments without the pipe convention skipped
- board detection fetches the job page once when the posting's own links give nothing away, capped at twelve a night
- close_unlisted, so removing a company closes the postings it brought
- the poll prints one line for unpollable companies instead of one line each
Broke:
- the first live run added 48 companies, nearly all sales, QA, marketing and backend roles, because discovery reused the scoring legs and those match api, automation and rendering; one HN comment became a company named after a whole sentence
Decided:
- discovery gets its own term list, tighter than scoring's, because a broad leg is right for a company already on the list and wrong for an open feed
- the 48 were deleted rather than triaged
Next:
- the second live night is the test of the tightened filter
