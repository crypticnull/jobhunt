# 2026-09-05, session twenty eight

The adversarial review. Eight lenses over the repo, three skeptics per finding, three site directions. 64 findings, 8 bugs proved by script, and the verify pass cut short by the usage limit, resumed the same evening.

Shipped:
- the remote gate reads the body: a country-only location is unclear, US residency phrases pass, "join us" is no longer nationwide, pay is gated on the lowest tier
- the product-motion titles tier, the product leg carries the curriculum vocabulary, plurals count, discovery learns the same words
- the nightly survives: the backup lands off-disk, a stale rebase is aborted, a dead feed is a line not a crash, 429s back off, `scraper drop` exists, the protocol is checked against the ruleset in CI
- the comp figures are out of the public protocol and the hook checks content, the letters choose the claim by role, the studio companies are off the list, the site proves skills the study list was assigning
- motion tokens with a reduced column, the chapter rail's mark, ADR-0012
Broke, mine:
- the first pass at the office rule would have made a city location remote on an office named Remote, the goal skeptic caught it before it shipped
Decided:
- the hero sentence, the resume title, scrubbing the figures from history and the Figma file are Matt's calls, listed in the review
Next:
- the tokens record, the contrast guard, the theme control, /system
