# 2026-09-02, session eighteen

Shipped:
- discovery decides on the job title now, after a real dry run on Matt's machine: 26 postings surfaced, 9 worth seeing, and every one of the 17 misses had an ordinary engineering or operations title
- generative ai demoted: it is boilerplate in software ads and was the only hit on 7 of the 17
- a strong-terms escape hatch, two distinct craft terms, so a real role behind an unstandardized title still lands
- reposts of the same role collapse to one, and photo editor, instructor and operations titles are excluded
Broke:
- bare redshift matched AWS Redshift despite the lookahead, so only redshift render survives; bare editor matched Real Estate Photo Editor
Decided:
- the feeds are unreachable from the sandbox, every host 403s at the egress proxy, so live verification runs on Matt's machine and comes back as a transcript
- both live runs are permanent test fixtures: the 45 companies of the first and the 26 postings of the second
Next:
- the third check is the nightly run, whose list and digest push to the repo
