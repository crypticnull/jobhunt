# 2026-09-02, session twenty five

The second import. Slug guessing found 45 boards on its own, every one of the
45 additions, including Anthropic, OpenAI, Stripe, Monks, Elastic, Roblox and
ElevenLabs. 88 companies polled, 6,090 open postings, zero errors. Discovery
held at 1.9% selectivity across 468 feed postings.

Shipped, all three from that run:
- .gitattributes forcing CRLF on cmd, bat and ps1. The Windows scripts were committed with bare newlines, and cmd.exe mis-parses those, which is where "'*' is not recognized" came from. setup.cmd also passes the time explicitly rather than through %*.
- the scheduled tasks register through schtasks with a task XML file. Register-ScheduledTask returned Access denied because replacing an existing task through the cmdlet wants elevation, and the error was non-terminating so the try/catch never fired and the script claimed success. The XML carries every setting the cmdlet would have and needs no elevation.
- the review pile is capped at 40 a week. Six thousand postings would otherwise produce a digest nobody reads, and marking them all surfaced would bury them for good. Held-back rows are not marked, so they come round again.
Broke:
- setup.ps1 pulls new code and then keeps running its own pre-pull copy, so a change to setup.ps1 itself only takes effect on the following run. That is why last-run.json was not committed this time.
Next:
- Matt: run setup once more so the schedule actually registers with its settings
