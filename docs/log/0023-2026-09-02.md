# 2026-09-02, session twenty three

Shipped:
- the nightly job moved to 04:00 and the time became an argument to setup.cmd rather than a hardcoded 02:30, because Matt is sometimes still working at half two
- both jobs registered at Windows scheduler priority 9, the lowest offered, so an overnight render or a game always wins the CPU
Decided:
- the job is a few minutes of waiting on HTTP, so priority is the honest answer rather than throttling anything
