# 2026-09-02, session twenty seven

The schedule is live: nightly at 04:00, status Ready, on Matt's machine. It
registered through the plain fallback rather than the task XML, so the
catch-up, priority and sign-in settings did not take.

Shipped:
- the likely cause: a LogonTrigger with no UserId means any user who signs in, which needs elevation. The current user is named on both the principal and the trigger now.
- registration degrades in steps rather than all at once: full task, then the same without the sign-in trigger since that is the fussiest part, then the plain form. The middle rung keeps catch-up and priority, which matter more.
- whatever schtasks refuses is printed instead of swallowed, so the next failure names itself.
Broke, mine:
- the first attempt at this patch missed most of its replacements because the file is CRLF and the patterns were LF. Redone with the line endings handled and an assertion per replacement.
