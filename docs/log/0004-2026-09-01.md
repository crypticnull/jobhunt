# 2026-09-01, session four

Shipped:
- scoring.json and its schema, score.py with per-rule reasons and a ruleset version on every row
- salary.py, comp from description text when the ATS gives none, hourly annualized and noted
- digest.py, three lanes plus source health, surfaced-and-unchanged stays quiet, terminal states stay out
- poll scores on the way in, digest and score commands, Task Scheduler install script
Broke:
- first borderline test expected a senior remote role to be borderline, it scores strong, the test was wrong
Decided:
- comp absence is its own digest lane, never a reason to drop
- the comp band lives only in data/local/scoring.local.json, the public rules carry no figures
Next:
- voice lint, milestone 5, then letters
