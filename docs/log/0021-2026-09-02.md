# 2026-09-02, session twenty one

The first real import. 168 companies in, 34 boards found by reading the
careers pages, 1,472 postings polled, and discovery read 464 feed postings
and kept 9, a 1.9% selectivity against the 100% of the first night.

Shipped, all four from what that run exposed:
- the push retries after a rebase: main moved while the poll ran, so the first push was refused
- slug variants learned from the real board names, lumaai and higgsfieldai and odysseyml, so the marketing words come off and four shapes are tried instead of two
- a board named twice on the list is polled once, after Figma was fetched twice
- the nightly and weekly jobs got the same push retry, since they run unattended
Broke:
- nothing, but Matt ran the import minutes before the slug guessing merged, so the 134 companies it could not place were never tried as slugs
Next:
- Matt: run setup again to put the guessing to work on those 134
