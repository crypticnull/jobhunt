# 2026-09-01, session eight

Shipped:
- docs/naming.md, the token spec, and pipeline/naming.py that refuses with the corrected form
- pipeline/media.py, image sizes from headers with no library, ffprobe and ffmpeg for video
- pipeline/frontmatter.py, a flow-style frontmatter reader and rewriter that leaves every other line alone
- pipeline/ingest.py, a named drop becomes hero, video, stills and process fields, video never enters the repo
Broke:
- a new hero beside the old placeholder let the placeholder win, hero and poster are singletons now
- the suggestion heuristic turned spaces into hyphens and lost the year, spaces are separators now
Decided:
- ingest owns four fields and regenerates them from disk, alt and captions survive, nothing else is touched
- one bad name refuses the whole drop, a half-ingested project is worse than none
Next:
- the first real asset drop, then encode and upload for R2 once the domain exists
