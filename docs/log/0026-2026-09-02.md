# 2026-09-02, session twenty six

Broke, mine, twice in one path:
- setup.ps1 passed -BackupDir with an empty default. PowerShell drops an empty string argument to -File, so the switch arrived with nothing after it and install-schedule.ps1 refused to start. The arguments are built as a list now and the optional one is only added when it has a value.
- last night's fixes were committed to the branch and never merged, so Matt's pull said "already up to date" and he ran the old code again.
Shipped:
- schedule.cmd, which installs or repairs the two tasks in seconds without polling, and prints the next run time so the result is visible rather than asserted
