# 2026-09-02, session twenty four

Shipped:
- a logon trigger on the nightly job, ten minutes after sign-in, because a task without a stored password only runs for a logged-on user and a night spent signed out would otherwise wait for the next clock trigger
- data/last-run.json, written by every poll and pushed nightly, and a digest banner when the last poll is two days old or more
Decided:
- no stored password and no SYSTEM account: SYSTEM cannot reach Matt's git credentials, and a stored password breaks the day he changes it. A logon trigger plus StartWhenAvailable covers the same ground and cannot rot.
