# 2026-09-01, session five

Shipped:
- letters/voicelint.py, twelve rules, two profiles, path:line:col output, inline waivers
- data/voice/rules.json, the lexicon and profiles as diffable config
- one passing and one failing sample per rule, and make lint in CI
Broke:
- the exclusion list only matched at the repo root, so the first run linted site/node_modules
- my own proof stories: one not-X-but-Y and one 43-word sentence, both rewritten
Decided:
- the repo profile bans only em dashes and the corporate lexicon, docs keep their parentheses
- the letter profile treats warnings as blocking, since a letter is the one place the voice must hold
Next:
- letter blocks and the brief command, milestone 7, once the first stills land
