# 2026-09-02, session twenty two

Shipped:
- the scheduled jobs catch up: registered through Register-ScheduledTask with StartWhenAvailable and WakeToRun, so a workstation asleep at half two runs its poll at the next opportunity rather than losing the day, with a schtasks fallback that says plainly when it could not
Decided:
- nothing is typed after setup: the jobs pull before they run and push what they produce
